import React from 'react';

type Props = {};

const CharityMainContent = (props: Props) => {
    return (
        <div className="h-full w-full">
            <span></span>
        </div>
    );
};

export default CharityMainContent;
